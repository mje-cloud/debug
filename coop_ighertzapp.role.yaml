name: "coop ighertzapp"
endpoints:
  - endpoint: /common/v1/typelists
    methods:
      - GET
  - endpoint: "/common/v1/typelists/*"
    methods:
      - GET
  - endpoint: "/admin/v1/users/*"
    methods:
      - GET
  - endpoint: /claim/v1/claims
    methods:
      - GET
      - POST
  - endpoint: "/claim/v1/claims/*"
    methods:
      - GET
      - PATCH
  - endpoint: "/claim/v1/claims/*/activities"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/contacts*"
    methods:
      - GET
  - endpoint: "/claim/v1/claims/*/contacts"
    methods:
      - GET
      - POST
  - endpoint: "/claim/v1/claims/*/contacts/*"
    methods:
      - GET
      - PATCH
  - endpoint: "/claim/v1/claims/*/documents"
    methods:
      - GET
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests"
    methods:
      - GET
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*"
    methods:
      - GET
      - PATCH
  - endpoint: "/claim/v1/claims/*/service-requests/*/accept"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/add-quote"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/approve-quote"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/assign"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/cancel"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/complete-work"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/decline"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/internal-cancel"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/invoices"
    methods:
      - GET
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/invoices/*"
    methods:
      - GET
      - PATCH
  - endpoint: "/claim/v1/claims/*/service-requests/*/invoices/*/approve"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/invoices/*/mark-as-paid"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/invoices/*/withdraw"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/service-requests/*/submit"
    methods:
      - POST
  - endpoint: "/claim/v1/claims/*/policy"
    methods:
      - GET
  - endpoint: "/claim/v1/claims/*/vehicle-incidents"
    methods:
      - GET
  - endpoint: "/coop/claim-ext/v1/*/create-rentalvendor-ext"
    methods:
      - POST
  - endpoint: "/common/v1/hertz-rental-status-history-accs-ext"
    methods:
      - POST
  - endpoint: /composite/v1/composite
    methods:
      - POST
accessibleFields:
  "*":
    edit:
      - "*public"
      - "*internal"
    view:
      - "*public"
      - "*internal"